*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
**/build/
app/schemas/
*.hprof
hs_err_pid*.log
replay_pid*.log
core.[0-9]*

# S2.8: real signing secrets — keystore.properties.example is the template
keystore.properties
*.keystore
*.jks

# S3.2: cloud-sync build config (Clerk publishable key + API base URL) — the
# compiled APK carries them, git never does. berilo.properties.example is the template.
berilo.properties
